version: '3.8'

services:
  server:
    build: .
    ports:
      - 1114:1114
    deploy:
      replicas: 1
      resources:
        limits:
          cpus: '0.50'
          memory: 50M
        reservations:
          cpus: '0.25'
          memory: 20M
