version: '3'

services:
  nginx:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - 1103:80

    depends_on:
      - php-fpm
    links:
      - php-fpm
    volumes:
      - ./challenge/:/var/www/html/

    
  php-fpm:
    image: php:8-fpm
    expose:
      - 9000
    volumes:
      - ./challenge/:/var/www/html/

